//Import dependencies
import express from 'express';

//Create an instance of express
const app = express();
let PORT = 3000;

//Middleware

//Routes

//Listen
app.listen(PORT, () => {
    console.log(`Server is running on port ${PORT}`)
})